# BACKLOG - santh-ctlog

Seeded 2026-07-14 (main-thread audit; Gemini swarm was quota-blocked). Well-built two-layer crate (pure parse + `fetch` feature) with strong parse/URL tests. Findings are fetch-layer hardening and test gaps. Rows: `file:line | category | severity | problem | acceptance criteria`.

DONE 2026-07-14 | test-gap | medium | The fetch layer's OOM guard (`CT_RESPONSE_MAX_BYTES` chunk-abort at 1381) — the entire reason karyx was folded onto this crate — has NO unit test. `discover_subdomains_ct*` is only "exercised by each consumer's integration tests", so the load-bearing `ResponseTooLarge` path can silently regress. | add a `wiremock`/mock-transport test that streams >32 MiB and asserts `Err(CtError::ResponseTooLarge{..})`, plus a success test under the cap. Assert the cap value, not just is_err. | FIXED: `CT_RESPONSE_MAX_BYTES` chunk-abort is now covered by `tests::fetch_tests::aborts_body_beyond_32mib_cap` using a local `wiremock` server streaming a body one byte over the cap; the test asserts `CtError::ResponseTooLarge { limit, got }` with `limit == CT_RESPONSE_MAX_BYTES` and `got > limit`. Added `wiremock` dev-dependency and `[lib] name = "ctlog"` so doctests and tests can `use ctlog::...`. `cargo +1.92.0 test -p santh-ctlog --features fetch` CARGO_EXIT=0.
DONE 2026-07-14 | capability | medium | No retry/backoff: `discover_subdomains_ct` does a single GET. crt.sh 502s and hangs frequently (the `BadStatus` doc admits it), so a transient 502 = total discovery failure for callers that use the convenience wrapper (only scald keeps its own retry via the pure layer). | add bounded retry/backoff on 502/transport in the fetch layer (Tier-A configurable attempts), or document loudly that retry is the caller's responsibility and why. | FIXED: added bounded retry/backoff in `discover_subdomains_ct_with`: `CT_MAX_RETRIES=3` and `CT_RETRY_BACKOFF=500ms` (linear), retrying `CtError::Transport` and `BadStatus(5xx/429)`. Exposed `CT_MAX_RETRIES`, `CT_RETRY_BACKOFF` and a `pub(crate)` `discover_subdomains_ct_with_url` test seam. Added `tests::fetch_tests::retries_502_then_succeeds` and `aborts_body_beyond_32mib_cap` using a local `wiremock` server. `cargo +1.92.0 test -p santh-ctlog --features fetch` CARGO_EXIT=0.
DONE 2026-07-14 src/lib.rs:1344 | config | low | `CT_RESPONSE_MAX_BYTES` (32 MiB) and `CT_QUERY_TIMEOUT` (30 s) are compile-time consts with no per-call override; a fleet lib should let a caller tune the size cap/timeout (Tier A). `discover_subdomains_ct_with` honors the client's timeout but the byte cap is fixed. | add an options struct / param so callers can override the byte cap (and document the default), instead of a hardcoded const. | DONE 2026-07-14: added `CtOptions { max_response_bytes: usize }` in `fetch_impl`, re-exported at crate root, plus `discover_subdomains_ct_with_options(client, domain, options)`. `discover_subdomains_ct_with` delegates to the new function with `CtOptions::default()`. `try_query` uses `options.max_response_bytes` for the `ResponseTooLarge` check. Added `fetch_tests::options_response_byte_cap_is_overridable` asserting a custom 1-byte cap aborts and a 1024-byte cap allows a small JSON response. `cargo +1.92.0 test --features fetch` in `libs/scanner/ctlog` passed (26 passed).
DONE 2026-07-14 src/lib.rs:1251 | coverage-gap | low | The query URL uses only the `%.` SQL-LIKE wildcard, so a domain that has ONLY an apex cert with no subdomain SANs returns nothing from crt.sh; apex-only certs can be missed by `discover_subdomains_ct`. | document the apex-only limitation, or issue a second `q=<domain>` query (or `%25.`+bare) and union, with a test. | DONE 2026-07-14: `discover_subdomains_ct_with_options` now issues both the wildcard `%.domain` query and an apex `domain` query, unions and deduplicates the results. Added `crtsh_apex_query_url`, pure tests `apex_query_url_targets_the_apex_identity` and `parse_subdomains_includes_apex_only_cert_subdomains`, and fetch test `fetch_unions_wildcard_and_apex_query_results` using a local mock server with `CtOptions.base_url` override. `cargo +1.92.0 test --features fetch` passed (29 passed).
DONE 2026-07-14 src/lib.rs:1113 | capability | low | crt.sh is the single CT source; when crt.sh is down (common) there is no fallback aggregator (certspotter, Censys). Single point of failure for the whole CT-discovery capability. | consider a pluggable second CT source behind the `fetch` feature; at minimum document the single-source risk. | DONE 2026-07-14: `CtOptions` gained `base_url`, making the crt.sh endpoint pluggable; added a single-source risk note to `discover_subdomains_ct`/`discover_subdomains_ct_with` pointing callers to `discover_subdomains_ct_with_options` and an alternate crt.sh-compatible mirror. The `fetch_unions_wildcard_and_apex_query_results` test exercises `CtOptions::base_url` against a local mock server. `cargo +1.92.0 test --features fetch` passed (29 passed).
DONE 2026-07-14 | src/lib.rs:1393 | silent-transform | low | `String::from_utf8_lossy(&body)` replaces invalid UTF-8 with U+FFFD before parsing; a corrupted body may parse into subtly wrong host strings rather than failing. Comment frames it as intentional degrade-to-parse-error. | pin the intended behavior with a test feeding invalid UTF-8 and asserting the outcome (parse error vs lossy host), so "lossy is fine" is proven not assumed. | FIXED: replaced `String::from_utf8_lossy` with `String::from_utf8` in `fetch_impl::try_query`; invalid UTF-8 now returns `CtError::Parse(serde_json::Error::custom(...))` instead of silently creating bogus hostnames with U+FFFD. Added `fetch_tests::rejects_invalid_utf8_response` using a `wiremock` server that returns an invalid-UTF-8 JSON body and asserts a `CtError::Parse`. `cargo +1.92.0 test -p santh-ctlog --features fetch --lib` green (CARGO_EXIT=0).
src/lib.rs:229 | santh-ctlog | performance | low | REPOPULATE 2026-07-16: normalized_names collects split lines into a temporary Vec for every JSON entry, causing needless heap allocation per entry in a hot loop | FIXED (Law-7): replaced the `flat_map(|e| e.name_value.split('\n').map(..).collect::<Vec<_>>())` (one throwaway Vec per CT-log entry - thousands for a busy domain) with a plain nested loop that keeps `e.name_value` alive across the inner iteration and pushes normalized names straight into the result, fusing the `!empty && !contains('*')` filter into the walk. (Returning the mapped iterator directly is impossible - it borrows the owned `name_value` that drops at the closure end; the nested loop is the borrow-correct zero-intermediate-alloc form.) Output is byte-identical (still sort+dedup). Proving test tests::multiple_multiline_entries_flatten_trim_normalize_and_dedup (3 entries, multi-line/mixed-case/whitespace/wildcard -> one sorted+deduped set). Gate: cargo test (default) 25 passed AND cargo test --features fetch 30 passed, both EXIT=0. | status=done
src/lib.rs:446 | santh-ctlog | build+coherence | medium | FOUND+FIXED 2026-07-17 (surfaced while gating the row above): `cargo test` with default features failed to COMPILE - `#[cfg(test)] pub(crate) use fetch_impl::discover_subdomains_ct_with_url;` was gated only on `test`, but `fetch_impl` exists only under `feature = "fetch"`, so the default test build errored `E0432 unresolved import fetch_impl`. The pure parse/URL test suite could not run without `--features fetch`. | FIXED: gate changed to `#[cfg(all(test, feature = "fetch"))]` (only the feature-gated `fetch_tests` module consumes it). Both `cargo test` (default, 25 passed) and `cargo test --features fetch` (30 passed) now compile and pass, EXIT=0. | status=done
DONE 2026-08-06 src/lib.rs:125 | input-sanitization | low | `crtsh_query_url`, `crtsh_apex_query_url`, and `parse_crtsh_subdomains` did not strip leading `*.` or `.` / trailing `.` from input domains, creating malformed query URLs like `q=%.%2A.example.com` and failing to exclude the apex when queried with `*.example.com`. | add `clean_domain` helper to strip leading `*.`/`.` and trailing `.`, update URL functions, parse_crtsh_subdomains, and discover_subdomains_ct_with_options, and strip trailing dots from SAN entries in normalized_names. | FIXED: added `clean_domain` in `src/lib.rs`, updated URL builders and subdomain filters, added trailing dot stripping for SAN normalization, and added 4 unit tests covering input sanitization and trailing dot handling. Both `cargo test -p santh-ctlog` (34 passed) and `cargo test -p santh-ctlog --features fetch` (39 passed) pass cleanly.